---
import MostrarImagenes from "./MostrarImagenes.astro";
const respuesta = await fetch("https://rickandmortyapi.com/api/character");
const { results } = await respuesta.json();
console.log(results);
const div = document.querySelector("div");
---

<section id="dibujos">
  <h1>Dibujos</h1>
  <figure>
    <MostrarImagenes elementos={results} />
    <!-- {
      results.map((elemento) => (
        <a href={`/dibujos/${elemento.id}`}>
          <img src={elemento.image} alt="" width="200px" />
          <p>{elemento.name}</p>
        </a>
      ))
    } -->
    <div class="divClass"></div>
  </figure>
</section>

<style>
  . section {
    height: 100vh;
  }

  h1 {
    text-align: center;
  }
  a {
    color: white;
  }

  figure {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  }
</style>
